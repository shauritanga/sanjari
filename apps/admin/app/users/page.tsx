import { AdminShell } from '../../src/components/AdminShell';

export default function UsersPage() {
  return <AdminShell><h1>User search</h1><p>Search, review, suspend, ban, export, and privacy actions require explicit permissions.</p></AdminShell>;
}
