import { AdminShell } from '../../src/components/AdminShell';

export default function MatchingPage() {
  return (
    <AdminShell>
      <h1>Matching configuration</h1>
      <p>Matching weights are transparent, versioned, and testable.</p>
    </AdminShell>
  );
}
