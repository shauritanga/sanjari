import { AdminShell } from '../../src/components/AdminShell';

export default function DeletionPage() {
  return (
    <AdminShell>
      <h1>Data-deletion requests</h1>
      <p>Deletion requests follow retention, evidence preservation, and legal hold rules.</p>
    </AdminShell>
  );
}
